(function () {
  const a = document.querySelector('.faq'); a.addEventListener('click', (b) => {
    let c = b.target,
      d = c.parentNode,
      f = d.querySelectorAll('.answer'); c.classList.contains('question') && f.forEach((g) => { d === g.parentNode && g.classList.toggle('disabled'); });
  });
}());
